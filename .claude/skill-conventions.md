# Project skill conventions

Slot values for the central skill set, one file per project. Required
slots have no default; optional slots are omitted here to fall back to
the documented defaults (see `SKILLS-MAINT.md` for the full schema).

Generated by `bootstrap-conventions` on 2026-06-04.

---

```yaml
# Identity
USER_NAME: Stephen
PROJECT_NAME: image-tools-mcp

# Build & test
BUILD_COMMAND: |
  go build ./... && go vet ./...
TEST_COMMAND: |
  make test
CANONICAL_TEST_TARGET: Linux dev container (arm64), mirroring the ubuntu-latest / ubuntu-24.04-arm CI runners

# Build version
BUILD_VERSION_LOCATION: VERSION
BUILD_VERSION_KEY: (whole file — bare semantic-version string; read by release.yml via `cat VERSION`)
BUILD_VERSION_EXAMPLE: 1.2.11

# Doc paths
PLAN_DIR: DOCs/plans/
PLAN_ARCHIVE_DIR: DOCs/plans/archive/          # gitignored — completed plans move here, not tracked
ANALYSIS_DIR: DOCs/
PUNCH_LIST_DOC: DOCs/plans/PUNCH-LIST.md
RELEASE_NOTES_DOC: CHANGELOG.md
SPEC_DOC: DOCs/IMAGE_MCP_BLUEPRINT.md

# Audience & vocabulary
RELEASE_NOTES_AUDIENCE: developers integrating the MCP server
```

## Omitted slots (defaults apply)

- **Optional doc guides** — `STYLE_GUIDE_DOC`, `HELP_VOICING_GUIDE`,
  `MANUAL_VOICING_GUIDE`: no such docs exist (no UI / in-app help / operator
  manual in this CLI/server project). Their steps are skipped.
- **Filename patterns** — no prior sprint artifacts to diverge from; the
  documented defaults (`<NAME>-SPRINT-PLAN.md`, etc.) apply.
- **Per-task detail artifacts** (`TASK_DETAIL_DIR` / `TASK_DETAIL_TEMPLATE`),
  **`DOC_RENDER_COMMAND`**, **`MODEL_TIERS` / `DEFAULT_MODEL`**,
  **`TEST_FLEET_DESCRIPTION`**, **`PROJECT_INIT_DATE`** — not needed; defaults
  fit.
- **P2 development cycle** — omitted; this is a Go MCP server, not a Parallax
  Propeller 2 project (no `.spin2` sources).
